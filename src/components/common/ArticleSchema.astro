---
/**
 * JSON-LD BlogPosting structured data for blog articles.
 * Generates schema.org/BlogPosting markup for Google rich results.
 *
 * Bez datePublished/dateModified (decyzja Wojtka 2026-09-06): artykuły są evergreen,
 * a Google pokazuje w wyniku wyszukiwania datę wziętą z danych strukturalnych nawet
 * wtedy, gdy strona jej nie wyświetla. Data z frontmatteru porządkuje tylko listing.
 */
export interface Props {
  title: string;
  description?: string;
  image?: string;
  url: string;
}

const { title, description, image, url } = Astro.props;

const schema = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: title,
  description: description || '',
  author: {
    '@type': 'Person',
    name: 'Wojciech Tracichleb',
    url: 'https://pozytywniezbudowani.pl/o-mnie/',
  },
  publisher: {
    '@type': 'Organization',
    name: 'Pozytywnie Zbudowani',
    logo: {
      '@type': 'ImageObject',
      url: 'https://pozytywniezbudowani.pl/images/logo-pz.svg',
    },
  },
  mainEntityOfPage: {
    '@type': 'WebPage',
    '@id': url,
  },
  mentions: [
    {
      '@type': 'Book',
      name: 'Od Marzenia Do Wprowadzenia – Budowa domu jednorodzinnego',
      author: { '@type': 'Person', name: 'Wojciech Tracichleb' },
      url: 'https://pozytywniezbudowani.pl/ksiazka/',
      publisher: { '@type': 'Organization', name: 'Vultima sp. z o.o.' },
      isbn: '9788397505506',
    },
  ],
  ...(image ? { image } : {}),
};
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
